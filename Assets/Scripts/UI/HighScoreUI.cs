using TMPro;
using UnityEngine;

public class HighScoreUI : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //ScoreManager.Instance.highScoreText = GetComponent<TMP_Text>();
    }
}
